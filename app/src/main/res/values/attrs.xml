<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>

    <declare-styleable name="DatePickerD">
        <attr name="yearPaddingLeft" format="dimension" />
        <attr name="yearPaddingRight" format="dimension" />
        <attr name="monthPaddingLeft" format="dimension" />
        <attr name="monthPaddingRight" format="dimension" />
        <attr name="dayPaddingLeft" format="dimension" />
        <attr name="dayPaddingRight" format="dimension" />
        <attr name="internalLayout" />
    </declare-styleable>
</resources>
